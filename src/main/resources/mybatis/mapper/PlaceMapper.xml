<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.gt.mapper.PlaceMapper">

    <!-- Result Map -->
    <resultMap id="PlaceResultMap" type="com.ssafy.gt.domain.Place">
        <id property="id" column="id"/>
        <result property="tag" column="tag"/>
        <result property="x" column="x"/>
        <result property="y" column="y"/>
        <result property="name" column="name"/>
        <result property="address" column="address"/>
        <result property="imageUrl" column="image_url"/>
        <result property="thumbnailUrl" column="thumbnail_url"/>
        <result property="averageRating" column="average_rating"/>
        <result property="tagType" column="tag_type"/>
    </resultMap>

    <!-- Insert -->
    <insert id="insert" parameterType="com.ssafy.gt.domain.Place" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO places (tag, x, y, name, address, image_url, thumbnail_url, average_rating)
        VALUES (#{tag}, #{x}, #{y}, #{name}, #{address}, #{imageUrl}, #{thumbnailUrl}, #{averageRating})
    </insert>

    <!-- Select by ID -->
    <select id="selectById" parameterType="int" resultMap="PlaceResultMap">
        SELECT p.*, pt.type as tag_type
        FROM places p
                 LEFT JOIN place_tags pt ON p.tag = pt.id
        WHERE p.id = #{id}
    </select>

    <!-- Select by Tag -->
    <select id="selectByTag" parameterType="int" resultMap="PlaceResultMap">
        SELECT p.*, pt.type as tag_type
        FROM places p
                 LEFT JOIN place_tags pt ON p.tag = pt.id
        WHERE p.tag = #{tag}
    </select>

    <!-- Select by Location (거리 기반 검색) -->
    <select id="selectByLocation" resultMap="PlaceResultMap">
        SELECT p.*, pt.type as tag_type,
               (6371 * acos(
                       cos(radians(#{y})) * cos(radians(p.y)) *
                       cos(radians(p.x) - radians(#{x})) +
                       sin(radians(#{y})) * sin(radians(p.y))
                       )) AS distance
        FROM places p
                 LEFT JOIN place_tags pt ON p.tag = pt.id
        <where>
            <if test="tag != null">
                AND p.tag = #{tag}
            </if>
        </where>
        HAVING distance &lt;= #{dist}
        ORDER BY distance
    </select>

    <!-- Full-Text 검색으로 후보군 추출 -->
    <select id="findCandidates" resultMap="PlaceResultMap">
        SELECT p.*, pt.type as tag_type
        FROM places p
                 LEFT JOIN place_tags pt ON p.tag = pt.id
        WHERE MATCH(p.name, p.address) AGAINST(#{keyword} IN BOOLEAN MODE)
        <if test="tag != null">
            AND p.tag = #{tag}
        </if>
        LIMIT 1000
    </select>

    <!-- Select All -->
    <select id="selectAll" resultMap="PlaceResultMap">
        SELECT p.*, pt.type as tag_type
        FROM places p
                 LEFT JOIN place_tags pt ON p.tag = pt.id
    </select>

    <!-- Select All by Tag -->
    <select id="selectAllByTag" resultMap="PlaceResultMap">
        SELECT p.*, pt.type as tag_type
        FROM places p
                 LEFT JOIN place_tags pt ON p.tag = pt.id
        <where>
            <if test="tag != null">
                p.tag = #{tag}
            </if>
        </where>
    </select>

    <!-- Update Average Rating -->
    <update id="updateAverageRating">
        UPDATE places
        SET average_rating = #{averageRating}
        WHERE id = #{id}
    </update>
</mapper>