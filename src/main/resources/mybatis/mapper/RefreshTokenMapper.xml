<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.gt.mapper.RefreshTokenMapper">

    <!-- RefreshToken 저장 (이미 존재하면 업데이트) -->
    <insert id="save" parameterType="com.ssafy.gt.dto.RefreshToken">
        INSERT INTO refresh_token (user_id, token, expires_at)
        VALUES (#{userId}, #{token}, #{expiresAt})
        ON DUPLICATE KEY UPDATE
            token = #{token},
            expires_at = #{expiresAt},
            created_at = CURRENT_TIMESTAMP
    </insert>

    <!-- Token으로 RefreshToken 조회 -->
    <select id="findByToken" parameterType="String" resultType="com.ssafy.gt.dto.RefreshToken">
        SELECT id, user_id, token, expires_at, created_at
        FROM refresh_token
        WHERE token = #{token}
    </select>

    <!-- UserId로 RefreshToken 조회 -->
    <select id="findByUserId" parameterType="String" resultType="com.ssafy.gt.dto.RefreshToken">
        SELECT id, user_id, token, expires_at, created_at
        FROM refresh_token
        WHERE user_id = #{userId}
    </select>

    <!-- RefreshToken 삭제 -->
    <delete id="deleteByUserId" parameterType="String">
        DELETE FROM refresh_token
        WHERE user_id = #{userId}
    </delete>

    <!-- 만료된 RefreshToken 삭제 -->
    <delete id="deleteExpiredTokens">
        DELETE FROM refresh_token
        WHERE expires_at &lt; NOW()
    </delete>

</mapper>