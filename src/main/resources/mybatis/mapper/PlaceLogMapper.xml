<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.gt.mapper.PlaceLogMapper">

    <!-- Result Map -->
    <resultMap id="PlaceLogResultMap" type="com.ssafy.gt.dto.PlaceLog">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="log" column="log"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- Insert -->
    <insert id="insert" parameterType="com.ssafy.gt.dto.PlaceLog">
        INSERT INTO place_log (user_id, log)
        VALUES (#{userId}, #{log})
    </insert>

    <!-- Select by User ID (최근 10개) -->
    <select id="selectById" parameterType="int" resultMap="PlaceLogResultMap">
        SELECT id, user_id, log, created_at
        FROM place_log
        WHERE id = #{id}
        ORDER BY created_at DESC
        LIMIT 10
    </select>

</mapper>
