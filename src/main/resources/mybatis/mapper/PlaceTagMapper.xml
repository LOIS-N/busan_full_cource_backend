<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.gt.mapper.PlaceTagMapper">

    <!-- Result Map -->
    <resultMap id="PlaceTagResultMap" type="com.ssafy.gt.domain.PlaceTag">
        <id property="id" column="id"/>
        <result property="type" column="type"/>
    </resultMap>

    <!-- Select by ID -->
    <select id="selectById" parameterType="int" resultMap="PlaceTagResultMap">
        SELECT * FROM place_tags WHERE id = #{id}
    </select>

    <!-- Select All -->
    <select id="selectAll" resultMap="PlaceTagResultMap">
        SELECT * FROM place_tags
    </select>

</mapper>